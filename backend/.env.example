# Django Backend Environment Variables (.env)
# Create this file in backend/ directory
# NEVER commit this to git!

# Core Settings
SECRET_KEY=your-secret-key-here-min-50-chars-random
DEBUG=False
ALLOWED_HOSTS=api.abhishek-kushwaha.com.np,abhishek-kushwaha.com.np,www.abhishek-kushwaha.com.np

# Database (PostgreSQL on Railway)
DATABASE_URL=postgresql://user:password@host:5432/dbname

# CORS Configuration
CORS_ALLOWED_ORIGINS=["https://abhishek-kushwaha.com.np","https://www.abhishek-kushwaha.com.np"]

# Email Configuration (Gmail)
EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=abhishekkushwaha.np@gmail.com
EMAIL_HOST_PASSWORD=your-16-char-app-password
DEFAULT_FROM_EMAIL=abhishekkushwaha.np@gmail.com

# Security (Production)
SECURE_SSL_REDIRECT=True
SESSION_COOKIE_SECURE=True
CSRF_COOKIE_SECURE=True

# Optional: Sentry for error tracking (add later if needed)
# SENTRY_DSN=your-sentry-dsn-here
