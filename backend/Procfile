web: gunicorn backend.wsgi --bind 0.0.0.0:$PORT --log-file -
# Ensure Pillow available in release phase before migrations
release: python -m pip install --no-cache-dir Pillow==11.0.0 && python manage.py migrate
