web: /app/.venv/bin/gunicorn backend.wsgi --bind 0.0.0.0:$PORT --log-file -
# Ensure Pillow available in release phase before migrations (use venv)
release: /app/.venv/bin/python -m pip install --no-cache-dir Pillow==11.0.0 && /app/.venv/bin/python manage.py migrate
